diff --git a/hyvideo/commons/__init__.py b/hyvideo/commons/__init__.py
index 4dde227..2061a29 100644
--- a/hyvideo/commons/__init__.py
+++ b/hyvideo/commons/__init__.py
@@ -137,7 +137,7 @@ def maybe_fallback_attn_mode(attn_mode):
             attn_mode = 'torch'
     elif attn_mode == 'flash3':
         if not is_flash3_available():
-            attn_mode = 'torch'
+            attn_mode = 'flash2' if is_flash2_available() else 'torch'
     elif attn_mode == 'flash2':
         if not is_flash2_available():
             attn_mode = 'torch'
diff --git a/hyvideo/models/transformers/hunyuanvideo_1_5_transformer.py b/hyvideo/models/transformers/hunyuanvideo_1_5_transformer.py
index 23f92b3..fd0deb6 100644
--- a/hyvideo/models/transformers/hunyuanvideo_1_5_transformer.py
+++ b/hyvideo/models/transformers/hunyuanvideo_1_5_transformer.py
@@ -958,7 +958,12 @@ class HunyuanVideo_1_5_DiffusionTransformer(ModelMixin, ConfigMixin, PeftAdapter
         """
         from peft.utils import get_peft_model_state_dict
 
-        from diffusers.loaders.lora_base import LORA_ADAPTER_METADATA_KEY, LORA_WEIGHT_NAME, LORA_WEIGHT_NAME_SAFE
+        from diffusers.loaders.lora_base import LORA_WEIGHT_NAME, LORA_WEIGHT_NAME_SAFE
+        try:
+            from diffusers.loaders.lora_base import LORA_ADAPTER_METADATA_KEY
+        except ImportError:
+            # Older diffusers (<0.33) lacks this constant; use the standard value.
+            LORA_ADAPTER_METADATA_KEY = "lora_adapter_metadata"
         from diffusers.utils import get_adapter_name
         import safetensors
         import json
diff --git a/train.py b/train.py
index 241d8f6..28c41ff 100644
--- a/train.py
+++ b/train.py
@@ -1769,42 +1769,42 @@ class HunyuanVideoTrainer:
         if self.world_size > 1:
             dist.barrier()
         
-        if self.config.use_lora and hasattr(self.transformer, "save_lora_adapter"):
-            has_adapters = bool(getattr(self.transformer, "peft_config", None))
-            if self.world_size > 1:
-                has_adapters_tensor = torch.tensor(
-                    int(has_adapters), device=self.device, dtype=torch.int32
-                )
-                dist.all_reduce(has_adapters_tensor, op=dist.ReduceOp.MIN)
-                has_adapters = bool(has_adapters_tensor.item())
-
-            if not has_adapters:
-                raise RuntimeError(
-                    "No LoRA adapter found in the model on at least one rank; "
-                    "aborting checkpoint save to avoid distributed deadlock."
-                )
-
+        if self.config.use_lora and getattr(self.transformer, "peft_config", None):
+            # Export a standalone LoRA adapter. Under FSDP2 the adapter weights are
+            # sharded DTensors, so gathering them needs an ALL-RANKS collective.
+            # The old code called save_lora_adapter() under `if is_main_process`,
+            # i.e. rank0 alone entered the gather -> deadlock -> the platform SIGKILLed
+            # the job at the first save (step 500), leaving an empty checkpoint that
+            # then poisoned every resume. Gather the full (unsharded) state dict on
+            # EVERY rank; only rank0 writes the LoRA subset. Best-effort + non-fatal:
+            # the dcp.save(transformer) below is the source of truth for resume/export.
+            from torch.distributed.checkpoint.state_dict import StateDictOptions
+            full_sd = get_model_state_dict(
+                self.transformer,
+                options=StateDictOptions(full_state_dict=True, cpu_offload=True),
+            )
             if self.is_main_process:
-                lora_dir = os.path.join(checkpoint_dir, "lora")
-                os.makedirs(lora_dir, exist_ok=True)
-                
-                if hasattr(self.transformer, "peft_config") and self.transformer.peft_config:
-                    adapter_names = list(self.transformer.peft_config.keys())
-                    logger.info(f"Saving {len(adapter_names)} LoRA adapter(s): {adapter_names}")
-                    
-                    for adapter_name in adapter_names:
-                        adapter_dir = os.path.join(lora_dir, adapter_name)
+                try:
+                    from safetensors.torch import save_file
+                    for adapter_name in list(self.transformer.peft_config.keys()):
+                        adapter_dir = os.path.join(checkpoint_dir, "lora", adapter_name)
                         os.makedirs(adapter_dir, exist_ok=True)
-                        self.transformer.save_lora_adapter(
-                            save_directory=adapter_dir,
-                            adapter_name=adapter_name,
-                            safe_serialization=True,
-                        )
-                        logger.info(f"LoRA adapter '{adapter_name}' saved to {adapter_dir}")
-            
+                        lora_sd = {
+                            k: v.contiguous()
+                            for k, v in full_sd.items()
+                            if "lora_" in k and (f".{adapter_name}." in k or adapter_name == "default")
+                        }
+                        save_file(lora_sd, os.path.join(adapter_dir, "adapter_model.safetensors"))
+                        self.transformer.peft_config[adapter_name].save_pretrained(adapter_dir)
+                        logger.info(f"LoRA adapter '{adapter_name}' ({len(lora_sd)} tensors) saved to {adapter_dir}")
+                except Exception as e:
+                    logger.warning(
+                        f"LoRA adapter export failed (non-fatal; dcp transformer/ is the "
+                        f"source of truth for resume/export): {e}"
+                    )
             if self.world_size > 1:
                 dist.barrier()
-        
+
         # Save full model state dict
         model_state_dict = get_model_state_dict(self.transformer)
         dcp.save(
@@ -1856,7 +1856,7 @@ class HunyuanVideoTrainer:
         
         
         transformer_dir = os.path.join(checkpoint_path, "transformer")
-        if os.path.exists(transformer_dir):
+        if os.path.exists(os.path.join(transformer_dir, ".metadata")):
             model_state_dict = get_model_state_dict(self.transformer)
             dcp.load(
                 state_dict={"model": model_state_dict},
@@ -1865,10 +1865,10 @@ class HunyuanVideoTrainer:
             if self.is_main_process:
                 logger.info("Transformer model state loaded")
         else:
-            logger.warning(f"Transformer dcp checkpoint not found from {checkpoint_path}")
+            logger.warning(f"Transformer dcp checkpoint missing/incomplete (no .metadata) at {transformer_dir}; skipping model load")
 
         optimizer_dir = os.path.join(checkpoint_path, "optimizer")
-        if os.path.exists(optimizer_dir):
+        if os.path.exists(os.path.join(optimizer_dir, ".metadata")):
             optimizer_state_dict = get_optimizer_state_dict(
                 self.transformer,
                 self.optimizer,
@@ -2203,7 +2203,16 @@ def create_dummy_dataloader4(config: TrainingConfig, dp_rank: int = 0, dp_size:
 
     ``sampled_frames`` / first-frame numpy for the condition stream are derived in ``prepare_batch``
     from ``input_video_path`` (see multitask_utils-style helpers), not from the collated batch alone.
+
+    Real data: set ``V2V_LATENTS_DIR`` to the directory produced by
+    ``v2v_data/preencode_v2v.py``; otherwise falls back to random dummy samples.
     """
+    latents_dir = os.environ.get("V2V_LATENTS_DIR")
+    if latents_dir:
+        from v2v_data.v2v_dataset import V2VEditDataset
+        dataset = V2VEditDataset(latents_dir)
+        return _make_dataloader(dataset, config, dp_rank, dp_size)
+
     class DummyDataset4:
         def __init__(self, size=100):
             self.size = size
